<h1> Mountains </h1>
<p><%= link_to 'Add New Mountain', new_mountain_path %></p>
<% @mountains.each do |mountain| %>
    <h3><%= mountain.name %></h3>
    <h3>Elevation: <%= mountain.elevation %></h3>
    <h3>Lat/Long: <%= mountain.latitude %> - <%= mountain.longitude %></h3>
    <h3>Difficulty rating: <%= mountain.rating %></h3>
    <%= link_to 'Show Mountain', mountain_path(mountain) %>
    <%= link_to 'Edit Mountain', edit_mountain_path(mountain) %>
    <%= link_to 'Delete Mountain', mountain_path(mountain), method: :delete %>

<% end %>
